# Python Scripts

This directory contains Python scripts for data processing:

- **eda.py** - EDA script version (can run without Jupyter)
- **load_data_to_postgres.py** - Database population script

## Usage

Run from the `src/` directory:

```bash
cd src
python scripts/load_data_to_postgres.py
```
